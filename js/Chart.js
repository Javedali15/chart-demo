'use strict';

drawLine();
drawBar();
drawDoughnut();



function drawLine() {
    var ctx
    var myChart = new Chart(ctx, {
        //declare type of chart to be used
        //type: 
        
        //declare data
        //data: {
            
    });
}
    

function drawBar() {
    //Use previous code, try changing the chart's "type" to display a bar chart
}

function drawDoughnut() {
    //Using previous code display a doughnut chart
}
